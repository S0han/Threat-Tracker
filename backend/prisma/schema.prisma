generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Threat {
  id            Int       @id @default(autoincrement())
  host          String
  url           String
  threat_type    String    @map("threat_type")
  date_added     DateTime  @map("date_added")
}